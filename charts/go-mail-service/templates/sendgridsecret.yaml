{{- if .Values.provider.sendgrid.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Values.provider.sendgrid.secret.name }}"
  labels:
    {{- include "go-mail-service.labels" . | nindent 4 }}
stringData:
  apiKey: "{{ .Values.provider.sendgrid.apiKey | default "" }}"
{{- end }}
